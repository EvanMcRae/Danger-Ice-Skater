using System;

namespace Waves {
    [Serializable]
    public enum EnemyType {
        BASIC,
        LINEAR,
        CIRCULAR,
        PROJECTILE,
        TRAP,
        SPECIAL,
        BOSS
    }
}